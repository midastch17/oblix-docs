{
  "name": "oblix-docs",
  "version": "0.1.0",
"scripts": {
  "build": "pip3 install --user mkdocs-material mkdocs-redirects && export PATH=/vercel/.local/bin:$PATH && mkdocs build"
},
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}
